import styled from 'styled-components';

export const HeaderStyle = styled.div`
  height: 40vh;
  background: hsl(210deg, 30%, 8%);
  border-radius: 4px;
  text-align: center;
  position: relative;
`;
