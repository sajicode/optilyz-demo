import React from 'react';
import { HeaderStyle } from '../styles';
import Search from './Search';

const Header = () => {
  return (
    <HeaderStyle>
      <Search />
    </HeaderStyle>
  );
};

export default Header;
